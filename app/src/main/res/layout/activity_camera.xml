<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout
    android:id="@+id/wysuwane_menu_identyfikacja_ulic"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include layout="@layout/activity_camera_main_content"/>

    <ListView
        android:id="@+id/lista_elementow_identyfikacja_ulic"
        android:layout_width="240dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/szary"
        android:choiceMode="singleChoice"
        android:divider="@color/czarny"
        android:dividerHeight="1dp"/>

</android.support.v4.widget.DrawerLayout>